# I-32 is the first gameplay consumer of CMF scripted effects and journal injects.
# Tiger must load CMF or it reports create_com_progress_bar / hide_com_progress_bar
# as unknown-field and CMF gui paths as missing-file.

languages = {
	check = "english"
	check = "turkish"
}

filter = {
	show_vanilla = no
	show_loaded_mods = no
}

load_mod = {
	label = "CMF"
	workshop_id = 3385002128
}
